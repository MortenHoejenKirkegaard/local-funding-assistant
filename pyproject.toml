[project]
name = "local-funding-assistant"
version = "0.1.0"
description = "Local funding assistant foundations."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
test = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

