funding_screening:
  enabled: true
  frequency: weekly
  day_of_week: monday
  local_time: "07:00"
  timezone: "Europe/Copenhagen"
  reason: "Soft funding calls do not need high-frequency polling."

notifications:
  deadline_reminders:
    - days_before_deadline: 30
    - days_before_deadline: 14
    - days_before_deadline: 7
    - days_before_deadline: 3
